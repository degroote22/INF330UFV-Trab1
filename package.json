{
  "name": "trabgrafos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^8.0.26",
    "typescript": "^2.4.2"
  },
  "scripts": {
    "build:cli": "tsc cli/index.ts",
    "run:cli": "cd cli && node index.js",
    "cli": "npm run build:cli && npm run run:cli"
  }
}